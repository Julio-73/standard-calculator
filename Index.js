

// h1
    ScrollReveal({
         reset: true,
         distance: '100px',
         duration:900,
         delay:400
         });

         ScrollReveal().reveal('.cal', { origin: 'right' });

// form
         ScrollReveal({
            reset: true,
            distance: '20px',
            duration:500,
            delay:200
            });

            ScrollReveal().reveal('form', { origin: 'bottom' });